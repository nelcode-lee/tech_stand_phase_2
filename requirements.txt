# RAG ingestion & API
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Vector store: Supabase (pgvector via vecs)
vecs>=0.4.0

# Document registry: raw SQL for documents table
psycopg2-binary>=2.9.0

# Embeddings
openai>=1.12.0

# Local dev (load .env for scripts)
python-dotenv>=1.0.0

# File text extraction for upload (DOCX, PDF, legacy .doc on Windows)
python-docx>=1.1.0
pypdf>=4.0.0
pywin32>=306; sys_platform == "win32"
